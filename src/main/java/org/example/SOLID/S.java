package org.example.SOLID;

public class S {
    // Assume SuperMarket

    public long buy() {
        // Generates aisles
        return 0;
    }

    // Should be in EmployeeService
    public long stock(String item, long amount) {
        // stock an item
        return amount;
    }

    // Should be ManagerService
    public void payEmployee() {
        //update transaction info in passbook
    }
}
